/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/anhwrkspx.github.io",
    output:"export",
    distDir:"dist",
    images:{
        unoptimized:true,
    },
}

module.exports = nextConfig
